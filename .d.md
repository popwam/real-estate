# AUTH-2 - Invitations and Phone Login Status

Date: 2026-06-24

## Summary

AUTH-2 fixed the organization invitation path and added safe phone + password login through the existing `POST /auth/login` endpoint. Existing email + password login remains compatible because the endpoint still accepts `email` as an alias.

## Current Invitation Flow

- Platform users with `organizations.verify` can create organization invitations from the Admin organization dossier.
- Invitations store organization, email, intended role, token hash, status, expiry, creator, and acceptance metadata.
- The raw token is returned only at creation time as an Admin Web `/invite/{token}` link.
- Invitation links are manual-share links. Email delivery is not claimed or implemented.
- Accepting an invitation validates token, status, and expiry, then sets a password hash and assigns the intended organization role.
- Expired and reused tokens fail safely.

## Invitation Root Cause

The invitation model and API existed, but the flow had two practical blockers:

- The invite URL fallback used `http://localhost:3001`, while the expected local Admin Web port is `3203`.
- Accepting an invite always conflicted when a matching user already existed, even if that user was a passwordless pending user shell in the same organization.

## Invitation Fix

- Invite URL generation now uses `ADMIN_WEB_URL`, then `ADMIN_WEB_BASE_URL`, then `http://localhost:3203`.
- Invite creation returns `delivery: "MANUAL_LINK"` and Admin copy says the link must be shared manually.
- Pending passwordless users in the same organization can now be linked by the invite acceptance flow.
- Existing password-backed users and users in another organization remain protected.
- Invite acceptance records an audit event.
- Invite acceptance can set a normalized optional phone number for future phone login.

## Phone Login Backend Status

Implemented through:

```text
POST /auth/login
```

Supported payloads:

```json
{ "identifier": "owner@example.com", "password": "secret" }
```

```json
{ "identifier": "+201001234567", "password": "secret" }
```

Backward-compatible payload:

```json
{ "email": "owner@example.com", "password": "secret" }
```

## Security Checks

- Passwords are verified with the existing password hash service.
- Phone login never uses code-only authentication.
- Login failures use generic `Invalid login details.` copy.
- User `isActive` is checked.
- Organization `SUSPENDED` and `REVOKED` statuses are blocked on login, refresh, and `/auth/me`.
- `/auth/login` existing rate limiting remains in place.
- Auth login success/failure is audit logged without storing the submitted identifier.
- Phone numbers are normalized before storage where phone is set by registration, invitation acceptance, or Admin user create/update.
- Application-level phone uniqueness is enforced for newly set phone numbers.

## Database Migration

No Prisma migration was applied.

Current limitation: `User.phone` exists but is not database-unique and has no normalized/indexed column. The service layer enforces uniqueness when setting phone and rejects ambiguous phone login matches, but production should add a normalized unique phone field or partial unique index before broad phone-login rollout.

## Admin UI Status

- Admin login label is now `Email or phone`.
- Organization invitations clearly state manual link sharing.
- Invitation acceptance page includes labeled fields, optional phone, secure password setup, success/error states, and an unavailable-token state.
- Admin does not show or set plaintext passwords outside the invitation acceptance flow.

## Mobile Status

- Mobile login label is now `Email or phone`.
- Mobile sends `{ identifier, password }` to `/auth/login`.
- Secure token storage and `/auth/me` validation remain unchanged.
- Wrong login failures stop loading and show safe error copy.

## Tests Added

Backend:

- Auth email login compatibility.
- Phone login success.
- Wrong password.
- Unknown phone.
- Inactive user.
- Suspended organization.
- Invitation creation.
- Manual invite link.
- Expired invitation.
- Reused invitation.
- Role assignment.
- Pending passwordless user linking.

Mobile:

- Login screen exposes `Email or phone`.
- Auth controller returns to signed-out state on login failure.

## Remaining Blockers

- Database-level unique normalized phone is still needed before production scale.
- Phone verification is not implemented.
- Email/SMS delivery for invitations is not implemented.
- Admin user management still distinguishes HR employee records from login users; employee-code login remains a separate backend-led feature.

# Employee Access Audit

Date: 2026-06-24

## Summary

AUTH-2 improves login-capable employee access through invitations and phone + password login. HR employee records remain operational records and are still not the same as authenticated users.

## Current Support

Supported:

- Email + password login.
- Phone + password login through `/auth/login` using `identifier`.
- Organization invitations with expiring token links.
- Invitation acceptance with secure password hashing.
- Optional phone capture during invitation acceptance.
- Organization roles and permissions assigned during invitation acceptance.
- Admin user creation/update with application-level phone uniqueness.

Not supported:

- Employee-code login.
- Code-only login.
- Phone verification.
- Email or SMS invite delivery.
- HR employee record as automatic login identity.

## HR Employee vs Login User

HR employees are managed through operations HR endpoints and represent staff records. Login users are records in `User` with an email, optional normalized phone, password hash, organization, role, and permissions.

For demo and staging, use organization invitations or Admin user management for login-capable employees.

## Employee Code Login Plan

Do not implement employee-code login until a backend contract is approved.

Recommended future payload:

```json
{
  "organizationCode": "NORTHLINE",
  "employeeCode": "EMP-1007",
  "password": "secret"
}
```

Security requirements:

- Password hash only.
- Unique employee code per organization.
- Active organization and active user checks.
- Rate limiting by IP, organization code, and employee code.
- Audit log for success and failure.
- Lockout or cooldown after repeated failures.
- No plaintext passwords.
- No code-only login.

## Current Recommendation

Use email/phone + password for the current staging demo. Treat employee-code login as a separate backend-led slice after phone login receives database-level uniqueness and device QA.

# MOBILE-AUTH Diagnosis

Date: 2026-06-24

## Summary

The mobile app is native Flutter using Dio, Riverpod, GoRouter, and secure token storage. AUTH-2 keeps the no-infinite-spinner fix and updates mobile login to support email or phone as the login identifier.

## Current Mobile Auth Flow

1. User enters email or phone and password.
2. Mobile calls `POST /auth/login` with `{ identifier, password }`.
3. Mobile stores returned access and refresh tokens in secure storage.
4. Mobile calls `/auth/me`.
5. Mobile enters signed-in state and routes to `/marketplace/projects`.

## API Environment

Default API base URL:

```text
https://api-staging.popwam.com
```

Override:

```bash
flutter run --dart-define=API_BASE_URL=https://api-staging.popwam.com
```

## Phone Login Support

Mobile now supports:

- Email + password.
- Phone + password.
- Generic invalid-login error copy.
- Existing secure token storage.
- Existing `/auth/me` validation.
- Safe failure state with loading stopped.

## CORS Relevance

CORS is not relevant for native Flutter Android/iOS HTTP calls. It applies only to Flutter web or a future WebView wrapper.

## Remaining Mobile QA

- Real Android/iOS staging login with email.
- Real Android/iOS staging login with phone.
- Wrong password.
- Unknown phone.
- API unreachable/offline.
- Stale token restore.
- App restart after login.
- Logout and login again.

# Release Candidate QA Report

Date: 2026-06-24

## AUTH-2 - Invitations and Phone Login

AUTH-2 fixed organization invitation behavior and added safe phone + password login for Admin and Mobile while keeping email + password login working.

### Files Changed

- `apps/api/src/common/phone-normalization.ts`
- `apps/api/src/modules/auth/auth.module.ts`
- `apps/api/src/modules/auth/auth.service.ts`
- `apps/api/src/modules/auth/auth.service.spec.ts`
- `apps/api/src/modules/auth/dto/login.dto.ts`
- `apps/api/src/modules/invitations/invitations.module.ts`
- `apps/api/src/modules/invitations/invitations.service.ts`
- `apps/api/src/modules/invitations/invitations.service.spec.ts`
- `apps/api/src/modules/platform-admin/platform-admin.service.ts`
- `apps/api/src/modules/platform-admin/platform-admin.service.spec.ts`
- `apps/api/src/modules/users/users.service.ts`
- `apps/admin-web/src/app/(app)/(auth)/login/login-form.tsx`
- `apps/admin-web/src/app/invite/[token]/page.tsx`
- `apps/admin-web/src/components/platform/organization-invitations-card.tsx`
- `apps/admin-web/src/lib/api.ts`
- `apps/admin-web/src/types/platform.ts`
- `apps/mobile/lib/core/errors/api_error.dart`
- `apps/mobile/lib/features/auth/data/auth_repository.dart`
- `apps/mobile/lib/features/auth/presentation/auth_controller.dart`
- `apps/mobile/lib/features/auth/presentation/login_screen.dart`
- `apps/mobile/test/auth_controller_test.dart`
- `apps/mobile/test/widget_test.dart`
- `apps/mobile/README.md`
- `AUTH_INVITATION_PHONE_LOGIN_STATUS.md`
- `EMPLOYEE_ACCESS_AUDIT.md`
- `MOBILE_AUTH_DIAGNOSIS.md`

### Result

- Invitation links now use the Admin Web base URL fallback `http://localhost:3203`.
- Invitation UI states that links are shared manually.
- Invitation acceptance can complete pending passwordless users in the same organization.
- Invitation acceptance sets password hashes only; no plaintext passwords are stored.
- `/auth/login` accepts `identifier` for email or phone and still accepts legacy `email`.
- Login failure copy is generic and does not expose whether a phone exists.
- Suspended/revoked organizations are blocked on login, refresh, and `/auth/me`.
- Mobile sends `{ identifier, password }` and keeps secure token storage.

### Remaining Release Risks

- No database-level unique normalized phone field yet.
- Phone verification is not implemented.
- Invite email/SMS delivery is not implemented.
- Full Admin browser smoke should be rerun after label change.

### Recommendation

Internal staging/demo can proceed after verification commands pass and a real staging phone-login account is seeded or assigned a unique phone. Production remains No-Go until normalized phone uniqueness is enforced at the database level and phone verification policy is decided.

# POPWAM Mobile

Flutter mobile client for POPWAM real estate workflows.

## API Environment

The app reads its API URL from the `API_BASE_URL` Dart define. If no value is supplied, it defaults to staging:

```bash
https://api-staging.popwam.com
```

Run against staging:

```bash
cd /d E:\saas\real-estate\apps\mobile
flutter run --dart-define=API_BASE_URL=https://api-staging.popwam.com
```

Run against a local API from the Android emulator:

```bash
cd /d E:\saas\real-estate\apps\mobile
flutter run --dart-define=API_BASE_URL=http://10.0.2.2:3000
```

Run against a local API from a physical device on the same network:

```bash
cd /d E:\saas\real-estate\apps\mobile
flutter run --dart-define=API_BASE_URL=http://<LAN-IP>:3000
```

Do not commit passwords, tokens, or private API URLs.

## Login

Mobile login uses one identifier field:

```json
{ "identifier": "owner@example.com", "password": "secret" }
```

or:

```json
{ "identifier": "+201001234567", "password": "secret" }
```

Tokens are stored with `flutter_secure_storage`, and the app validates the session with `/auth/me` after login.

## Checks

```bash
cd /d E:\saas\real-estate\apps\mobile
flutter analyze
flutter test
```
**Summary**
Implemented AUTH-2 with a safe, backward-compatible path: existing email/password login still works, `/auth/login` now also accepts `identifier` for email or phone, invitation acceptance can complete passwordless pending users, and Admin/Mobile now expose “Email or phone” login.

**Invitation root cause**
The invitation flow existed, but two practical blockers were found: invite links defaulted to `localhost:3001` instead of the expected Admin port, and acceptance failed if a passwordless pending user already existed for the invited email.

**Invitation fix**
Invite links now use `ADMIN_WEB_URL`, `ADMIN_WEB_BASE_URL`, then `http://localhost:3203`. Invitation UI says links are shared manually, not emailed. Acceptance validates token/expiry/reuse, hashes the password, assigns org role/permissions, can link a pending passwordless same-org user, and audits acceptance.

**Phone login backend result**
`POST /auth/login` now accepts:
```json
{ "identifier": "+201001234567", "password": "secret" }
```
It still accepts legacy:
```json
{ "email": "owner@example.com", "password": "secret" }
```
Login failures use generic `Invalid login details.` User active and organization suspended/revoked checks are enforced on login, refresh, and `/auth/me`.

**Phone login UI/mobile result**
Admin login and Mobile login now use “Email or phone”. Mobile sends `{ identifier, password }`, preserves secure token storage, validates via `/auth/me`, and wrong login stops loading with a safe error.

**Files modified**
API auth/invitations/users, Admin login/invite UI, Mobile auth/login, mobile README, and release docs were updated.

**Files created**
- `apps/api/src/common/phone-normalization.ts`
- `apps/api/src/modules/auth/auth.service.spec.ts`
- `apps/api/src/modules/invitations/invitations.service.spec.ts`
- `apps/api/src/modules/platform-admin/platform-admin.service.spec.ts`
- `apps/mobile/test/auth_controller_test.dart`
- `AUTH_INVITATION_PHONE_LOGIN_STATUS.md`

**Database migration needed**
Not applied. `User.phone` exists but is not unique or normalized at DB level. Production should add a normalized unique phone field/index before broad rollout.

**Tests added**
Backend tests for invite create/accept/expired/reused/role assignment, phone login success, wrong password, unknown phone, inactive user, and suspended org. Mobile test for failed login returning to signed-out state.

**Commands run/results**
Passed:
- `pnpm --filter api build`
- `pnpm --filter api test -- --runInBand`
- `pnpm --filter admin-web lint`
- `pnpm --filter admin-web build`
- `flutter analyze`
- `flutter test`
- `git diff --check` passed with CRLF warnings only.

**Remaining blockers**
Phone verification is not implemented. Invite email/SMS delivery is not implemented. DB-level normalized phone uniqueness is still needed for production.

**Staging/demo recommendation**
Go for internal staging/demo after assigning unique phone numbers to demo login users.

**Production recommendation**
No-Go until normalized phone uniqueness and phone verification policy are implemented.

**Next action**
Seed or update staging demo users with unique normalized phone numbers, then test Admin and Mobile login with both email and phone.
